export interface UploadAttachments {
  voucher_id:               string;
  rsbsa_no:                 string;
  reference_no:             string;
  transaction_id:           string;
  is_complete:              string; // is_complete = '0'
  supplier_id:              string;
}